# TimeBoard

```shell
获取代码
git clone https://github.com/baozhadepiqiu/TimeBoard.git

cd TimeBoard

使用以下命令安装依赖
npm: 
    npm install
pnpm: 
    pnpm install
yarn:
    yarn 
运行：
    npm run build

打开 http://localhost:3000/
```
